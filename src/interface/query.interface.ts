export interface IQuery {
  minPrice: string;
  maxPrice: string;
  releaseDate: string;
  brand: string;
  modelNumber: string;
  category: string;
  operatingSystem: string;
  connectivity: string;
  powerSource: string;
}
